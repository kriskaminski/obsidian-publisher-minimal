# Testing repo for mkdocs

## Testing obsidian style embeds (transclusions)

### setup

clone the repo

create venv

`python -m venv venv`

activate venv

`.\venv\Scripts\activate`

install requirements

`pip install -r requirements.txt`

serve site in development

`mkdocs serve`